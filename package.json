{
  "name": "lexicon",
  "version": "0.1.0",
  "private": true,
  "description": "Editor's vocabulary trainer — Postgres, Express, React.",
  "license": "UNLICENSED",
  "type": "module",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "migrate": "node --env-file=.env server/src/migrate.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "pg": "^8.22.0"
  },
  "devDependencies": {
    "@types/node": "^24.13.3",
    "@types/pg": "^8.21.0",
    "typescript": "^7.0.2"
  }
}
