{
  "name": "lexicon",
  "version": "0.1.0",
  "private": true,
  "description": "Editor's vocabulary trainer — Postgres, Express, React.",
  "license": "UNLICENSED",
  "type": "module",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "dev": "node --env-file=.env server/src/index.ts",
    "start": "node server/src/index.ts",
    "migrate": "node --env-file=.env server/src/migrate.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "express": "^5.2.1",
    "pg": "^8.22.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^24.13.3",
    "@types/pg": "^8.21.0",
    "typescript": "^7.0.2"
  }
}
